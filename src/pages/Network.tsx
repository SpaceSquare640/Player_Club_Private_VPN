/** Network page stub — peer connections and virtual LAN management. */
export default function Network() {
  return (
    <div className="space-y-4" data-testid="page-network">
      <div>
        <h1 className="text-2xl font-semibold text-ink">Network</h1>
        <p className="text-sm text-ink-muted">
          Peer connections and virtual LAN.
        </p>
      </div>
      <div className="rounded-2xl border border-dashed border-white/10 p-10 text-center text-sm text-ink-muted">
        Network management — coming soon.
      </div>
    </div>
  );
}
